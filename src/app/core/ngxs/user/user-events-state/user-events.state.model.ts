
export interface UserEventsStateModel {
  login: {
    errorMessage: string;
  };

  register: {
    errorMessage: string;
  };

  changePassword: {
    errorMessage: string;
  };

  checkUsername: {
    errorMessage: string;
  };

  returnAid: {
    errorMessage: string;
  };

  updateInfoPersona: {
    errorMessage: string;
  };
}
